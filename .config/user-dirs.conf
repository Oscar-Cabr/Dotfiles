# ~/.config/user-dirs.conf
#
# Stops xdg-user-dirs-update from ever rewriting ~/.config/user-dirs.dirs,
# should the xdg-user-dirs package land as a dependency. Under the es_US
# locale it would create a second, Spanish-named set of directories.
# The .dirs file is maintained by hand; see the comment there.
enabled=False
